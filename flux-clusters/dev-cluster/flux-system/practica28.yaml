apiVersion: apps/v1
kind: Deployment
metadata:
  name: practica28
  namespace: wave2
spec:
  replicas: 4
  selector:
    matchLabels:
      app: myapp
  template:
    metadata:
      labels:
        app: myapp
    spec:
      containers:
      - name: nginx
        image: nginx
